//! # gpu-npu-util-reporter 入口（脚手架占位）
//!
//! 后续任务将在此串联配置加载、日志初始化、--init 模式、schema 校验、
//! 调度与归档。当前仅为最小可编译占位，确保 CI 与依赖可正常拉取。

fn main() {
    // 脚手架占位输出，后续任务替换为完整流程。
    println!("gpu-npu-util-reporter scaffold");
}
